# Balori

A mobile app built with Expo SDK 54, React Native, and React Native Paper (Material Design 3).

## Setup

```bash
bun install
bun run start
```

Also works with `npm install` / `npm run start`.

## Development

See [CLAUDE.md](CLAUDE.md) for workflow rules and conventions.
